# Flags de la app

```
 --help       Show help                                            
  --version   Show version number                                   
  -b, --base  Es la base de la tabla de multiplicar    
  -l, --listar   Muestra la tabla en la consola      
  -h, --hasta    Hasta que numero se realizara la multiplicacion
```